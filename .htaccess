<IfModule mod_rewrite.c>
    RewriteEngine On

    # Browser URL must include /public/ when document root is the project folder.
    RewriteRule ^$ public/ [L,R=302]
    RewriteRule ^public/ - [L]
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
